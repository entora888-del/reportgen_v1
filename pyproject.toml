[project]
name = "reportgen"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "pyside6",
  "requests",
  "beautifulsoup4",
  "lxml",
  "pdfplumber",
  "pillow",
  "docxtpl",
  "python-docx",
  "geopandas",
  "pandas",
  "shapely",
  "pyproj",
  "fiona",
  "openai",
  # tabula-py, pytesseract は後で必要になったら追加
]

[tool.pyinstaller]
# 将来使う想定（spec化やhook化したい場合のメモ欄）
